
import { z } from "zod";

const createExpenseSchema = z.object({
	
});
const updateExpenseSchema = z.object({
	
});

export const ExpenseValidations = {
	createExpenseSchema,
    updateExpenseSchema,
};

